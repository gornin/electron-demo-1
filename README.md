# electron-demo-1

```shell
echo "# electron-demo-1" >> README.md 

git init

git add README.md

git commit -m "first commit" 

git branch -M main

git remote add origin git@github.com:gornin/electron-demo-1.git

git push -u origin main
```